# The Odds API (https://the-odds-api.com/) -- reuse the same key as the CFB model.
# Used only for live/upcoming line-movement snapshots (sport=americanfootball_nfl).
# All historical lines come free from nflverse, so this is the only secret.
ODDS_API_KEY=
